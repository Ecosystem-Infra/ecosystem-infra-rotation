on: pull_request
name: PR
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - run: export DEPLOY_SHA=`git rev-parse FETCH_HEAD^2`
    - run: env
    - run: npm install
    - run: node deploy.js
